package AkkaRemoting.P3_Clustering

import akka.actor.{Actor, ActorLogging, ActorRef}

class LoadBalancer extends Actor with ActorLogging {

  override def receive: Receive = ???

//  def balance(workers: List[ActorRef]): Receive = {
//    case
//  }
}
